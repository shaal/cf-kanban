services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: cfkanban
      POSTGRES_PASSWORD: cfkanban
      POSTGRES_DB: cfkanban
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
